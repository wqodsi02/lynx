[pytest]
# `pythonpath = .` mette la root del progetto in sys.path, cosi' i test possono
# fare `from backend...` senza bisogno di installare il pacchetto ne' di
# manipolare sys.path a mano nei conftest.
pythonpath = .
testpaths = tests
addopts = -v --strict-markers
